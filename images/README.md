# Images

Drop JPG/PNG/GIF files in this folder. The poster rotates through them in
alphabetical order (so `01-foo.png`, `02-bar.png` gives predictable order).

Keep them under ~5MB each. LinkedIn recommends 1200x627 for feed share images.

If this folder is empty, posts will go up text-only.

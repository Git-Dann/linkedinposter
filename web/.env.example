# GitHub — personal access token with content:write on your repo
GITHUB_TOKEN=
GITHUB_OWNER=Git-Dann
GITHUB_REPO=linkedinposter
GITHUB_BRANCH=main

# Anthropic — used for drafting posts
ANTHROPIC_API_KEY=

# Single password that gates the app
APP_PASSWORD=

# 32+ character random string. Generate with: openssl rand -base64 32
SESSION_PASSWORD=
